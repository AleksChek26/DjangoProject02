# Django Project 02 — Онлайн‑платформа обучения

### Многоконтейнерное веб‑приложение на Django REST Framework с использованием Docker Compose и Nginx.

## Реализованный функционал

### Проект представляет собой онлайн‑платформу обучения с модульной архитектурой на базе Docker Compose и обратным прокси Nginx. В системе реализованы следующие ключевые компоненты и функции:

## Основные модули

* **Бэкенд на Django + DRF**: RESTful API для управления учебным контентом, пользователями и процессами обучения.
* **Система аутентификации**: JWT‑токены для безопасной авторизации и управления сессиями.
* **Управление контентом**: CRUD‑операции для курсов, уроков, тестов и заданий.
* **Админ‑панель Django**: интерфейс для модераторов и администраторов платформы.
* **Фоновая обработка задач**: асинхронное выполнение ресурсоёмких операций через Celery.
* **Периодические задачи**: автоматическое обновление статистики, рассылка уведомлений через Celery Beat.
* **Кэширование**: ускорение доступа к часто запрашиваемым данным с помощью Redis.
* **Асинхронные уведомления**: отправка email‑уведомлений пользователям о новых материалах и событиях.
* **Статика и медиафайлы**: обслуживание через Nginx для повышения производительности.

### Технологический стек

* **Бэкенд**: Django + DRF (Django REST Framework)
* **Веб‑сервер**: Nginx (обратный прокси, обслуживание статики и медиа)
* **База данных**: PostgreSQL 15 (надёжное хранение структурированных данных)
* **Кэш и брокер сообщений**: Redis (быстрое кэширование и передача сообщений между сервисами)
* **Фоновые задачи**: Celery + Celery Beat (асинхронная обработка и планирование задач)
* **Контейнеризация**: Docker + Docker Compose (изолированная среда для каждого сервиса)
* **Управление зависимостями**: Poetry (удобное управление Python‑пакетами)

## Предварительные требования

### Перед началом работы убедитесь, что у вас установлены:

* Docker Desktop (с поддержкой WSL 2 для Windows)
* Python 3.12+
* Poetry
* Git (для клонирования репозитория)

## Установка и запуск

### Шаг 1. Клонирование репозитория

    git clone DjangoProject02

### Шаг 2. Создание файла .env

    Создайте файл .env на основе шаблона .env.example
    Отредактируйте .env, указав свои значения для чувствительных данных (секретные ключи, пароли и т. д.).

### Шаг 3. Сборка и запуск контейнеров
#### Выполните команду для сборки образов и запуска всех сервисов:

    docker-compose up --build

    Для запуска в фоновом режиме добавьте флаг -d:

    docker-compose up -d --build

### Шаг 4. Выполнение миграций БД
#### После запуска контейнеров выполните миграции базы данных:

    docker-compose exec web python manage.py migrate

### Шаг 5. Сбор статических файлов
#### Соберите статические файлы Django:

    docker-compose exec web python manage.py collectstatic --noinput

### Шаг 8. Создание суперпользователя (опционально)
#### Для доступа к админ‑панели Django создайте суперпользователя:

    docker-compose exec web python manage.py createsuperuser

## Проверка работоспособности сервисов
### Веб‑сервис (Django/DRF через Nginx)

#### Откройте в браузере:

   http://localhost
#### Должна загрузиться страница Django или интерфейс DRF API. Nginx должен обслуживать статику и проксировать запросы к Django.

### База данных (PostgreSQL)
#### Проверьте логи сервиса БД:

    docker-compose logs db
#### Убедитесь, что нет ошибок подключения.

### Redis
#### Проверьте доступность Redis:

    docker-compose exec redis redis-cli ping
#### Ожидаемый ответ: PONG.

### Celery (воркер)
#### Проверьте логи воркера Celery:

    docker-compose logs celery
#### В логах должны быть сообщения о запуске воркера и готовности принимать задачи.

### Celery Beat (планировщик)
#### Проверьте логи планировщика:

    docker-compose logs celery-beat
#### В логах должны быть сообщения о планировании периодических задач. Ищите строки вида:

    [INFO] Scheduler: Sending due task <имя_задачи>
    [INFO] DatabaseScheduler: Schedule changed.
#### Если планировщик не находит задачи, убедитесь, что:

    миграции выполнены (python manage.py migrate);

    периодические задачи зарегистрированы в админке Django или через код.

### Nginx
#### Проверьте логи Nginx:

    docker-compose logs nginx
#### Убедитесь, что нет ошибок конфигурации и запросы успешно проксируются к Django.

### Переменные окружения
#### Все чувствительные данные вынесены в файл .env.
#### Важно: не коммитьте файл .env в репозиторий. Он добавлен в .gitignore.

## Дополнительные команды
### Остановка всех контейнеров:

    docker-compose down
### Перезапуск конкретного сервиса (например, Nginx):

    docker-compose restart nginx
### Просмотр списка запущенных контейнеров:

    docker ps
### Очистка неиспользуемых образов и контейнеров (освобождение места):

    docker system prune
### Проверка конфигурации Nginx внутри контейнера:

    docker-compose exec nginx nginx -t

## Документация:

    Для получения дополнительной информации обратитесь в службу поддержки по телефонам указанным в контактах.

## Лицензия
    
     Проект выполнен по лицензии MIT

## Автор

### Чекунов Александр Николаевич
